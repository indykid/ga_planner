
<div class="box">

  <h5><%= @course.title %></h5>

  <h5>Description:</h5>
  <%= @course.description %>


  <h5>Price: £</h5>
  <%= @course.price %>


<h5>Days running on:</h5>
<ul>

<% @course.bookings.each do |b| %>

<li><%= b.b_date if b.b_date %></li>

<%= link_to 'Destroy', b, method: :delete, data: { confirm: 'Are you sure?' } %>

<%end%>
</ul>



<%= link_to 'Enroll', enroll_path(@course.id), class: "button" %>
<%= link_to 'Edit', edit_course_path(@course), class: "button" %>
<%= link_to 'Back', courses_path, class: "button"  %>



