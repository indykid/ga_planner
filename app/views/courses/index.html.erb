<h2>see what's on:</h2>



<% @courses.each do |course| %>
  <div class="box">
    <h4 class="rounded"><%= link_to course.title, course %></h4>
    <p><%= link_to course.description, course %></p>
    <p><%= course.start_date%> - <%= course.end_date%></p>
    <p>£<%= course.price %></p>
    <%= link_to 'ENROLL', enroll_path(course.id), class: "action_button rounded" %>
    </div>
    
<% end %>


<% if can? :create, Course %>
<div class="button clear rounded">
<%= link_to 'Add Course', new_course_path %>
</div>
<%end%>
